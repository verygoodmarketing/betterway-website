---
interface Props { 
  href: string
  label: string 
}
const { href, label } = Astro.props as Props
---

<a href={href} class="flex flex-col items-center gap-2 p-4 rounded-lg hover:bg-white/20 transition-colors">
  <span class="text-sm font-semibold tracking-wide text-white">{label}</span>
</a>

